name: burger_food
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.9
  
  image_cropper: ^5.0.1
  image_picker: ^1.0.4
  google_nav_bar: ^5.0.6
  bloc:
  flutter_bloc:
  meta: ^1.9.1
  firebase_core: ^2.24.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
